import { thumbHashToRGBA } from 'thumbhash';
import { c as base64ToBytes, r as rgbaToDataUri } from './shared/core.ae5b1847.mjs';

function createPngDataUri(hash) {
  const { w, h, rgba } = thumbHashToRGBA(base64ToBytes(hash));
  return rgbaToDataUri(w, h, rgba);
}

export { createPngDataUri };
